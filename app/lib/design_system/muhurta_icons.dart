/// Icon metrics — single source for affordance sizes across cards and tabs.
abstract final class MuhIcons {
  /// WhatsApp share glyph on card headers (Decode, Today, Life Map, Timing, etc.).
  static const double whatsAppShare = 18;

  /// InkWell padding around the share glyph for a comfortable tap target.
  static const double whatsAppSharePadding = 4;
}
